<% provide(:title, '投稿一覧') %>
<div id='indx-list' class='col-sm-8 col-md-8'>
  <% if @posts.present? %>
    <h4>投稿一覧</h4>
    <%= render partial: 'post', collection: @posts %>
    <%= paginate @posts %>
  <% else %>
    <h4>表示できる投稿が現在ありません。</h4>
  <% end %>
</div>